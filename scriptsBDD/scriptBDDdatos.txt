CREATE TABLE datos (no_control int(8) NOT NULL, nombre varchar(20) NOT NULL, apellidos varchar(40) NOT NULL, direccion varchar(50) NOT NULL, foto blob, telefono int(10) NOT NULL, correo varchar(50) NOT NULL, linkedin varchar(40) NOT NULL, aptitudes varchar(60) NOT NULL, universidad varchar(40) NOT NULL, carrera varbinary(30) NOT NULL, fin_estudio date NOT NULL, area_estudio varchar(30) NOT NULL, empresa1 varchar(150) NOT NULL, duracion1 varchar(30) NOT NULL, puesto1 varchar(150) NOT NULL, descripcion1 varchar(200) NOT NULL, empresa2 varchar(150), duracion2 varchar(30), puesto2 varchar(150), descripcion2 varchar(200), empresa3 varchar(150), duracion3 varchar(30), puesto3 varchar(150), descripcion3 varchar(200), pasantias varchar(50) NOT NULL, certificados varchar(255) NOT NULL, idioma1 varchar(30) NOT NULL, idioma2 varchar(30), idioma3 varchar(30), PRIMARY KEY (no_control));